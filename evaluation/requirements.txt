# Evaluation harness dependencies (separate from the main pipeline's reqs).
# Pin tightly so re-runs are reproducible. Install with:
#   pip install -r evaluation/requirements.txt

numpy==1.26.4
pandas==2.2.2
matplotlib==3.8.4
seaborn==0.13.2
scikit-learn==1.4.2
umap-learn==0.5.6
py-motmetrics==1.4.0
tabulate==0.9.0
PyYAML==6.0.1
pytest==8.2.0

# TrackEval has no PyPI release; install from GitHub if you want HOTA via the
# academic-standard implementation (recommended). Otherwise the harness falls
# back to py-motmetrics for HOTA-equivalents and CLEAR metrics.
#   pip install "git+https://github.com/JonathonLuiten/TrackEval@master"
